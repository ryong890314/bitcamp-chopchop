<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitcamp.chopchop.dao.QuestionDao">

  <resultMap type="Question" id="QuestionMap">
    <id column="question_no" property="no"/>
    <result column="name" property="name"/>
    <result column="email" property="email"/>
    <result column="title" property="title"/>
    <result column="contents" property="contents"/>
    <result column="enterDate" property="enterDate"/>
  </resultMap>
  
  <select id="findAll" resultMap="QuestionMap">
    select * from question
  </select>
  
  <insert id="insert" parameterType="Question">
    insert into question
    (
      email, 
      title, 
      contents, 
      enterDate
    )
    values 
    (
      #{email}, 
      #{title}, 
      #{contents}, 
      now() 
    )
  </insert>
  
  <select id="findBy" parameterType="int" resultMap="QuestionMap">
    select * from question
    where question_no = #{no}
  </select>
  
  <update id="update" parameterType="Question">
    update question set
      title = #{title}, 
      contents = #{contents} 
    where question_no = #{no}
  </update>
  
  <delete id="delete" parameterType="int">
    delete from question
    where question_no = #{no}
  </delete>

</mapper>